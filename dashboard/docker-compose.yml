services:
  dashboard:
    image: ece461l-hw4:dashboard
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - '.:/react-app'
      - '/react-app/node_modules'
    ports:
      - 3001:3000
    environment:
      - WATCHPACK_POLLING=true
      - WDS_SOCKET_PORT=3001